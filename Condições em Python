#Olá, aqui eu explico um pouco como utilizar as Condições if e else em Python.


#Como utilizar o if(SE) e o else(SENÃO).

nome = str(input('Qual é o seu nome: '))
if nome == 'Gustavo':    #O If é o SE em inglês.
    print('Que nome lindo você tem!')
else:      #O else é o SENÃO em inglês.
    print('Seu nome é tão normal')
print('Bom dia, {}'.format(nome))


#Cálculo de uma média: Primeira opção para escrever o programa, veja:
n1 = float(input('Digite a primeira nota: '))
n2 = float(input('Digite a segunda nota: '))
n = (n1 + n2) / 2
print('A suma média foi {:.1f}'.format(n))
if n>= 6.0:
     print('Sua média foi boa: Parabéns!')
else:
    print('Sua média foi ruim: Estude mais!')


#Segunda opção de escrever o programa, veja:
n1 = float(input('Digite a primeira nota: '))
n2 = float(input('Digite a segunda nota: '))
n = (n1 + n2) / 2
print('A suma média foi {:.1f}'.format(n))
print('Parabéns' if n >= 6 else 'Estude mais')

